import React from 'react';
import Directions from '../components/Directions';

const Game = props => {
   return (
      <>
         <p>Placeholder.</p>
         <Directions />
      </>
   );
};

export default Game;
