import React from 'react';
import Directions from '../Directions';
import Canvas from '../Canvas/Canvas';

const Game = props => {
   return (
      <>
         <Directions />
         <Canvas />
      </>
   );
};

export default Game;
